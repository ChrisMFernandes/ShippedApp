<div class="container">
	<div class="text-center">
		<h1>Jobs</h1>
	</div>

	 <table class="table table-striped">
    <thead>
      <tr>
        <th>Description</th>
        <th>Origin</th>
        <th>Destination</th>
        <th>Cost</th>
        <th>Containers</th>
      </tr>
    </thead>
    <tbody>
    	<% @jobs.each do |t| %>
      <tr>
        <td><%= t.description %></td>
        <td><%= t.origin %></td>
        <td><%= t.destination %></td> 
        <td><%= t.cost %></td>
        <td><%= t.containers %></td>
        <td><%= link_to "Edit Job", edit_job_path(t) %></td>
      </tr>
       <% end %>
    </tbody>
  </table>
	

</div>